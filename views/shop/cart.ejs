<%- include('../partials/header') %>

<div style="max-width:900px;margin:auto;padding:30px;">
    <h2 style="font-size:26px;margin-bottom:20px;">🛍 Your Cart</h2>

    <% if(cart.length === 0) { %>
        <p>Your cart is empty.</p>
        <a href="/" style="padding:10px 20px;background:black;color:white;border-radius:6px;text-decoration:none;">
            Back to Shop
        </a>
    <% } else { %>

    <table width="100%" cellpadding="12" style="border-collapse:collapse;">
        <tr style="background:black;color:white;">
            <th>Product</th>
            <th>Price</th>
        </tr>

        <% cart.forEach(p => { %>
            <tr>
                <td><%= p.name %></td>
                <td>Rs <%= p.price %></td>
            </tr>
        <% }) %>
    </table>

    <h3 style="margin-top:20px;font-size:22px;">Total: Rs <%= total %></h3>

    <a href="/checkout"
       style="display:inline-block;margin-top:15px;padding:12px 25px;background:green;color:white;
              font-size:18px;border-radius:6px;text-decoration:none;">
        Proceed to Checkout →
    </a>

    <% } %>
</div>

<%- include('../partials/footer') %>
